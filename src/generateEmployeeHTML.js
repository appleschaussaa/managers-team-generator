const generateMarkdown = ({data}) => {
   
    return `
    ${data.name}
  
    ${data.role}
  
    ${data.id}
    
    ${data.email}
  
    ${data.officeNumber}

    ${data.github}
  
    ${data.school}
  `;
};  